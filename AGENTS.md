# Untils

This file is for coding agents working with this project.

---

"Untils" lets users set up monitors for things they care about and get notified when they change.

It's a Go web application that stores data in PostgreSQL and serves up HTML templates with Templ. On the frontend it uses Tailwind 4 with Daisy UI, and HTMX.

## Development server

The development server will be running and uses hot reload as you make your changes. You can view its logs at ./dev.log - however just tail the last few lines
as this file could be very big.

You may use the Chrome MCP server to access the website at address http://localhost:4200/app and logging in with username "alexpls@fastmail.com" and password "abc123".
